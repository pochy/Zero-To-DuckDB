# 用語集

## DuckDB

アプリや分析環境の中で直接動く、分析処理向けのSQLエンジンです。サーバーを立てずに、CSVやParquetなどのファイルをSQLで読めます。

## OLTP

注文作成、ユーザー登録、在庫更新のように、小さな更新を大量に安全に処理する用途です。PostgreSQLやMySQLが典型です。

## OLAP

売上集計、ログ分析、品質チェックのように、大量データを読み、集計・分析する用途です。DuckDBはこの領域が得意です。

## embedded / in-process

DBサーバーへ接続するのではなく、Python、Node.js、CLIなどのプロセス内でDBエンジンが動く方式です。

## raw

入力データをなるべく元の形で保存する層です。後から原因調査できるように、最初から整えすぎません。

## staging

rawを分析しやすい形へ整える層です。型変換、trim、カテゴリ名の正規化、日付変換などを行います。

## mart

分析やアプリが使いやすい形にした最終層です。品質レポート、集計表、標準化済み商品テーブルなどが入ります。

## Parquet

分析向けの列指向ファイル形式です。必要な列だけ読みやすく、圧縮効率も高いため、DuckDBと相性が良い形式です。

## partition

データを日付やカテゴリなどの単位でディレクトリ分割することです。必要な範囲だけ読めるようにするために使います。

## schema

テーブル名、カラム名、型などの構造です。実務では「どの層に何を置くか」という設計も含めて考えます。

## `try_cast`

変換に失敗した値をエラーで止めずに `NULL` にする関数です。汚い入力データの品質チェックに向いています。

## quality check

欠損、重複、不正な価格、未知カテゴリなどを検出する処理です。データ処理では集計結果より先に品質を確認します。

## batch_id

どの取り込み単位で入ったデータかを示すIDです。再実行、差分処理、障害調査で重要になります。

## source_file

どのファイルから来た行かを示す情報です。DuckDBの `filename = true` はこの追跡に役立ちます。

## `EXPLAIN`

SQLをDuckDBがどう実行するかを見るための命令です。遅い処理を調べる入口になります。

## `httpfs`

HTTP、HTTPS、S3互換ストレージ上のファイルをDuckDBから読むための拡張機能です。

## SEMI JOIN / ANTI JOIN

`SEMI JOIN` は、相手テーブルに一致する行だけを残すJOINです。`ANTI JOIN` は、相手テーブルに一致しない行だけを残すJOINです。マスタに存在する商品だけを見る、マスタに存在しないカテゴリだけを検出する、といった品質調査に向いています。

## UNPIVOT

横持ちの列を縦持ちの行へ戻す操作です。`price_202601`、`price_202602` のような月別列を、`month` と `price` の2列に変換する時に使います。

## dimension / fact

dimensionは、商品、カテゴリ、メーカーのような分析の切り口です。factは、価格、数量、エラー件数のような観測値です。実務では、mart層をdimensionとfactに分けるとBIや分析から使いやすくなります。

## SCD

Slowly Changing Dimensionの略です。商品名、カテゴリ、メーカー名のように、ゆっくり変わる属性の履歴をどう持つかという考え方です。

## OCR confidence

AI-OCRやLLM抽出結果に付く信頼度です。低い値は必ず不正とは限りませんが、人間確認や追加チェックの優先度を上げるための品質信号になります。

## spill

メモリに収まりきらないJOIN、集計、ソートなどを、一時的にディスクを使って処理することです。DuckDBは大きな処理でもspillできますが、性能はSQL、データ量、一時領域に左右されます。

## auto reader

`read_csv_auto` や `read_json_auto` のように、DuckDBが型や構造を推論して読む関数です。初期調査には便利ですが、再実行するパイプラインでは読み込み条件を明示する方が安全です。

## star expression

`SELECT * EXCLUDE` や `SELECT * REPLACE` のように、`*` を拡張して列をまとめて扱うDuckDBの構文です。列数が多い表で便利ですが、後続が依存するSQLでは明示列の方がレビューしやすい場合があります。

## `UNNEST`

リストや構造体のようなネストした値を、行や列へ展開する操作です。JSONやAPIレスポンスを分析用の表へ変換する時に使います。

## `ASOF JOIN`

時点が完全一致しないデータを、指定した条件で近い過去の行に結合するJOINです。価格履歴、為替、マスタ履歴のような時系列データで使います。

## dbt / Dagster / dlt

DuckDBの周辺で使われることがあるデータツールです。dbtはSQLモデルとテスト、Dagsterはジョブ依存関係や再実行、dltはAPIやJSONの取り込みを支援します。このチュートリアルでは標準経路にせず、発展的な採用判断として扱います。
