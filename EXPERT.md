# DuckDBエキスパート編

このファイルは、Part 0-11を終えたあとに読むための発展編です。

Part 0-11を完了すると、DuckDBを使って実務寄りのデータ取り込み、品質チェック、正規化、Parquet出力、外部DB連携、簡単な運用設計まで説明できるようになります。これは初級ではありません。業務の小さなデータ処理を自分で設計できるという意味では、中級から上級の入口です。

ただし、それだけで「DuckDBのエキスパート」と言い切るにはまだ足りません。

エキスパートとは、DuckDBのSQLを多く知っている人ではありません。DuckDBをどこに置くべきか、どこに置くべきでないか、遅いときに何を見るか、壊れたときにどう切り分けるか、チームや本番運用でどのように安全に使うかを説明できる人です。

## エキスパートの定義

このチュートリアルでいうDuckDBエキスパートは、次の状態を指します。

- DuckDBをSQLite、PostgreSQL、BigQuery、ClickHouse、Spark、pandas、Polarsと比較して、採用理由と不採用理由を説明できる
- ファイル、DB、Notebook、CLI、バッチ、アプリの境界を設計できる
- `EXPLAIN`、プロファイル、入出力サイズ、Parquet設計、メモリ使用量から性能問題を調査できる
- データ品質、スキーマ変更、再実行、リトライ、監視、成果物のバージョン管理を含めてパイプラインを設計できる
- DuckDBの強みを活かしながら、中央DWH、Lakehouse、業務DB、分散処理基盤へ責務を逃がす判断ができる
- 事故が起きたときに、入力、変換、品質条件、出力、利用者影響を追跡できる

つまり、エキスパートに必要なのは「便利な関数を知っていること」ではなく、「DuckDBが成立する前提と限界を理解して、システム全体の中で責務を設計できること」です。

## なぜPart 11完了だけではエキスパートではないのか

Part 11までの最終課題では、商品情報統合パイプラインをローカルで動かします。CSV、Excel、JSON Lines、SQLite、PostgreSQL、HTTP、S3互換ストレージ、Parquetを扱い、品質チェックと出力も行います。

これはかなり実務に近い構成です。しかし、教材として制御された環境です。

実際の現場では、次のような問題が起きます。

- 入力ファイルの列名が予告なく変わる
- 価格、日付、カテゴリ、JANコードの意味が部署ごとに違う
- 同じメーカーから同じ商品が別IDで届く
- ある日のファイルだけ文字コードや区切り文字が違う
- S3上のファイルが途中で追加・削除される
- Notebookでは動くが、バッチではメモリ不足になる
- 開発者のローカルでは成功するが、CIや本番サーバでは拡張機能が入らない
- 品質チェックの期待値が「業務の改善」によって変わる
- 後続のBI、アプリ、ML処理が出力スキーマに依存し始める

エキスパートは、これらを「例外的なトラブル」とは考えません。むしろ、これらが起きる前提で処理を設計します。

## 1. DuckDB内部の理解

最初に深めるべき領域は、DuckDBがどのような思想で動いているかです。

DuckDBは、分析処理向けの組み込みSQLエンジンです。PostgreSQLのように常駐サーバを立てて多数のアプリケーションから同時更新を受けることを主目的にしていません。pandasのようにプログラム内のデータ構造を手続き的に操作するだけの道具でもありません。

DuckDBの強さは、列指向、ベクトル化実行、SQL最適化、ファイル直接読み取り、組み込み実行が組み合わさるところにあります。

学ぶべきこと:

- 行指向と列指向の違い
- ベクトル化実行がなぜ分析処理に効くのか
- クエリオプティマイザが何を並べ替え、何を推測するのか
- フィルタ、射影、JOIN、集計が実行計画上でどう扱われるのか
- メモリに乗らない処理で一時領域やspillがどう関係するのか
- トランザクションと同時実行におけるDuckDBの前提

なぜ必要か:

内部を知らないと、遅いSQLを見たときに「DuckDBは遅い」としか言えません。内部の考え方を知っていれば、不要列を読んでいるのか、フィルタが押し込まれていないのか、JOIN前に件数を減らせるのか、Parquetの物理設計が悪いのか、メモリ条件が合っていないのかを分けて考えられます。

エキスパートの到達基準:

- `EXPLAIN` を見て、処理の大きな流れを言葉で説明できる
- 「SQLの書き方」「データレイアウト」「実行環境」のどれが性能に効いているか切り分けられる
- DuckDBをサーバDBの代わりとして使う危険性を、内部構造の観点から説明できる

## 2. パフォーマンス設計

DuckDBの性能は、SQLだけで決まりません。入力ファイルの形式、Parquetのrow group、圧縮方式、パーティション、列数、ファイル数、ローカルディスクかネットワークか、メモリ制限、スレッド数によって変わります。

初心者は、遅いクエリを見るとSQLの書き換えだけで解決しようとします。しかし実務では、SQLよりもデータの置き方が支配的なことがあります。

学ぶべきこと:

- `EXPLAIN` と `EXPLAIN ANALYZE` の読み方
- DuckDBのprofiling出力
- Parquetのcolumn pruningとpredicate pushdown
- row group size、圧縮、統計情報、partitioned datasetの設計
- 小さすぎるファイルが大量にある場合のコスト
- JOIN前に件数と列数を減らす設計
- 一時テーブル、ビュー、CTE、マテリアライズの使い分け
- `memory_limit`、`threads`、一時ディレクトリの調整

なぜ必要か:

実運用では、入力が10MBのうちは問題が見えません。1GB、100GB、数千ファイルになったときに、読み取り方式、列選択、フィルタ順、ファイル構成の差が大きくなります。エキスパートは、クエリ単体ではなく、データの物理設計まで含めて性能を考えます。

エキスパートの到達基準:

- 遅い処理に対して、測定前に結論を出さない
- 処理時間、入力サイズ、出力サイズ、メモリ、実行計画をセットで記録できる
- Parquet datasetの設計変更が後続利用者へ与える影響を説明できる

## 3. 本番データパイプライン設計

DuckDBを業務で使うとき、重要なのは「1回動くSQL」ではなく「何度でも同じ意味で再実行できる処理」です。

実務のパイプラインでは、失敗します。入力が欠けます。再実行します。途中まで出力されます。後続が古い成果物を読みます。品質条件が変わります。この前提を受け入れると、設計は大きく変わります。

学ぶべきこと:

- batch id、run id、ingest timestampの設計
- rawデータを証拠として残す理由
- stagingで解釈し、martで契約を提供する理由
- 再実行可能性と冪等性
- 入力スナップショットと出力バージョン
- schema driftへの対応
- data contractと品質ゲート
- リトライ、ロールバック、再構築手順
- ログ、メトリクス、アラート、runbook

なぜ必要か:

DuckDBは軽量なので、ローカルの便利な変換処理として始まりやすいです。しかし、その成果物を他者が使い始めた瞬間に、処理は個人作業ではなくなります。出力Parquet、品質レポート、JSON結果、HTMLレポートは、すべて契約になります。

エキスパートの到達基準:

- 失敗した日だけを安全に再処理できる
- 入力と出力の対応を後から追跡できる
- 品質チェックの変更が、過去データと将来データにどう影響するか説明できる

## 4. Lakehouse / DWHとの役割分担

DuckDBは強力ですが、すべての分析基盤をDuckDBだけで作るべきではありません。

DuckDBは、ローカルまたはアプリ内での分析、ファイル処理、前処理、品質チェック、軽量なデータマート作成に強いです。一方で、大規模な同時利用、中央集権的な権限管理、全社DWH、分散処理、長期的なデータガバナンスは、別の基盤が適していることがあります。

学ぶべきこと:

- DuckDB、BigQuery、Snowflake、Redshift、ClickHouse、Trino、Sparkの役割差
- Parquet dataset、Hive partitioning、Iceberg、Delta Lake、DuckLakeの考え方
- object storageをデータレイクとして使うときの注意点
- カタログ、メタデータ、スキーマ進化
- ローカル分析と中央分析の境界
- どこまでDuckDBで前処理し、どこからDWHへ渡すか

なぜ必要か:

DuckDBは「小さく強い」道具です。だからこそ、中央基盤にするかどうかは慎重に判断します。良い設計では、DuckDBを孤立した便利ツールにせず、DWH、Lakehouse、業務DB、BI、MLパイプラインと役割を分けます。

エキスパートの到達基準:

- DuckDBを採用しない判断を説明できる
- DuckDBで作ったParquet成果物を、他の基盤から安全に読む設計ができる
- Lakehouse形式を使うべき場面と、単純なParquet datasetで十分な場面を分けられる

## 5. 外部システム連携

DuckDBは外部DB、HTTP、S3、Arrow、pandas、Polars、Python、Node.jsなどと連携できます。これは便利ですが、連携点は障害点でもあります。

外部連携で重要なのは、接続できることではありません。接続先の責務、権限、失敗時の扱い、スナップショット性、再現性を設計することです。

学ぶべきこと:

- PostgreSQL、SQLite、S3、HTTPを読み取り専用で扱う理由
- 外部DBを直接JOINするときのリスク
- 認証情報とsecret管理
- ネットワーク失敗、タイムアウト、部分的な読み取り失敗
- Python/Node.jsからDuckDBを呼ぶときの責務分担
- Arrow、pandas、Polarsへ渡す境界
- BIツールやNotebookから成果物を読むときの契約

なぜ必要か:

外部連携は、DuckDBの守備範囲を広げます。しかし、守備範囲を広げすぎると、どこで失敗したのか分からなくなります。エキスパートは、外部システムを「便利な入力」ではなく「信頼境界」として扱います。

エキスパートの到達基準:

- 読み取り専用、スナップショット、認証、再実行の設計を説明できる
- 外部DBへ負荷をかけすぎない読み取り設計ができる
- アプリからDuckDBを呼ぶとき、SQL処理とアプリ判断の境界を決められる

## 6. セキュリティとガバナンス

分析処理では、セキュリティが軽視されやすいです。理由は、処理がローカル、Notebook、CLI、個人環境から始まることが多いからです。

しかし、DuckDBはファイルを読み、外部DBへ接続し、S3へアクセスし、ParquetやCSVを出力します。つまり、データを簡単に複製できる道具です。

学ぶべきこと:

- 認証情報をSQLやNotebookに直書きしない設計
- 個人情報、機密情報、取引情報の扱い
- 出力ファイルの保存場所とアクセス権
- ローカルDBファイル、キャッシュ、一時ファイル、Notebook出力の残存
- 監査ログと処理履歴
- untrusted SQLを実行しない設計
- 開発環境、本番環境、共有環境の分離

なぜ必要か:

DuckDBは軽量なので、強力な処理を簡単に実行できます。この簡単さは長所ですが、データ持ち出しや権限逸脱のリスクにもなります。エキスパートは、DuckDBの便利さを保ちながら、データの境界を明確にします。

エキスパートの到達基準:

- secretをコード、SQL、Notebookから分離できる
- 出力成果物のアクセス範囲を設計できる
- 分析用コピーがどこに残るか説明できる

## 7. 拡張機能と高度な組み込み

DuckDBは拡張機能によって、JSON、Excel、httpfs、PostgreSQL、spatialなどの機能を追加できます。また、Python、Node.js、Wasm、Arrow、UDFなどを通じて、アプリケーションに深く組み込めます。

ここで重要なのは、「拡張できるから使う」のではなく、「どの責務をDuckDB内に入れるべきか」を判断することです。

学ぶべきこと:

- `INSTALL` と `LOAD` の運用上の違い
- ネットワーク制限環境での拡張機能管理
- UDFやcustom functionを使うべき場面
- SQLに業務ロジックを入れすぎる危険性
- Arrow、pandas、Polarsとのゼロコピーまたは低コピー連携
- Wasmや組み込み利用の制約
- 拡張機能のバージョンと再現性

なぜ必要か:

DuckDBはアプリ内に入れられるため、便利な処理をすべてDuckDB側へ寄せたくなります。しかし、業務ルール、UI判断、権限判断、外部API呼び出しまでSQL内に閉じ込めると、テストや変更が難しくなります。

エキスパートの到達基準:

- 拡張機能を本番で使うときのインストール、固定、検証方針を決められる
- SQL、UDF、アプリコードの責務境界を説明できる
- DataFrame連携を、便利さだけでなくメモリと所有権の観点から判断できる

## 8. 実データでの運用経験

最後に必要なのは、実データでの失敗経験です。

教材のデータは、学習しやすいように設計されています。実データはもっと曖昧です。列名、単位、欠損、重複、文字コード、カテゴリ体系、日付表現、ID体系、価格の意味、税込・税抜、タイムゾーン、マスタの履歴が揺れます。

学ぶべきこと:

- 入力元ごとの癖を記録する
- データ品質問題を欠陥ではなく業務知識として扱う
- マスタ変更、商品統廃合、カテゴリ再編へ対応する
- 期待値が変わるテストをどう管理するか
- 分析者、業務担当、アプリ開発者とデータ契約を合意する
- 障害時に、誰が何を見ればよいか決める

なぜ必要か:

エキスパートは、DuckDBの中だけで完結しません。入力を作る人、出力を使う人、運用する人、レビューする人の間に立ち、データの意味を技術的な処理へ落とし込みます。

エキスパートの到達基準:

- 品質チェックの失敗を、技術エラーと業務上の変化に分けて説明できる
- データの意味が変わったとき、SQLだけでなく契約と運用手順を更新できる
- 後続利用者に影響する変更を事前に検知・通知できる

## 推奨学習順

エキスパートを目指す場合、次の順で深めます。

### Phase 1: 内部構造と性能を固める

Part 8を起点に、`EXPLAIN`、profiling、Parquet設計、メモリ制限、ファイル数、JOIN戦略を学び直します。

演習:

- 同じ集計をCSV、単一Parquet、partitioned Parquetで比較する
- 不要列を含むクエリと必要列だけ読むクエリを比較する
- JOIN前にフィルタする場合とJOIN後にフィルタする場合を比較する
- row groupやpartition設計を変えて、実行計画と処理時間を見る
- `make perf-lab` を実行し、blocking operator、window処理、`preserve_insertion_order = false`、`memory_limit`、`threads` がどの観点で効くか観察する

### Phase 2: 本番パイプライン化する

Part 9とPart 11を起点に、再実行、run id、品質ゲート、出力バージョン、ログ、アラートを追加します。

演習:

- `run_id` と `batch_id` をすべてのraw/staging/martテーブルに持たせる
- 入力ファイルのmanifestを作り、どのファイルからどの出力が作られたか追跡する
- 品質チェックの結果を履歴化する
- 失敗時に途中成果物を利用しないように出力ディレクトリを設計する
- dimension/fact/SCDのmartを作り、BIや後続アプリが依存してよい契約にする

### Phase 3: 外部基盤との境界を設計する

Part 7とPart 10を起点に、外部DB、S3、DWH、Lakehouseとの責務分担を設計します。

演習:

- PostgreSQLマスタを直接参照する場合と、スナップショットしてから参照する場合を比較する
- S3上のParquet datasetを、パーティション設計を変えて読み比べる
- DuckDBで前処理し、DWHやBIへ渡す出力契約を文書化する
- DuckDBを使わない方がよいケースを3つ書く

### Phase 4: セキュリティ、拡張、組み込みを固める

アプリやバッチに組み込む場合、認証情報、権限、拡張機能、UDF、デプロイ環境を設計します。

演習:

- secretを環境変数またはsecret managerから読む形にする
- 拡張機能のインストール手順をネットワークあり/なしで分ける
- Python CLIとNode.js CLIの責務を整理し、SQLとの境界を文書化する
- untrusted SQLを受け取らないAPI設計を考える

## 自己評価チェックリスト

次の問いに自分の言葉で答えられるなら、エキスパートに近づいています。

- DuckDBをWebアプリのメインDBにしない理由を、OLTP/OLAPと同時実行の観点から説明できるか
- DuckDBがSQLite for Analyticsと呼ばれる理由を、組み込み性と分析実行の両面から説明できるか
- `EXPLAIN` を見て、どこで大量スキャン、JOIN、集計が起きているか説明できるか
- Parquetのpartition設計を、後続クエリの条件から逆算できるか
- raw/staging/martの境界を、監査性と変更容易性の観点から説明できるか
- 品質チェックに失敗した行を、消すのではなく追跡可能に残せるか
- 外部DBを直接読む場合の負荷、権限、スナップショット性を説明できるか
- DuckDB、pandas、Polars、Spark、DWHの役割を使い分けられるか
- 入力スキーマが変わったとき、どの層を変えるべきか説明できるか
- DuckDBを採用しない判断を、チームに説明できるか

## 到達レベルの目安

| レベル | 状態 |
| --- | --- |
| 初級 | DuckDB CLIでCSVを読み、基本SQLで集計できる |
| 中級 | 複数形式の入力を取り込み、正規化し、Parquetへ出力できる |
| 上級 | 品質チェック、外部連携、性能調査、運用設計を含むパイプラインを作れる |
| エキスパート | DuckDBの内部前提、限界、外部基盤との責務分担、本番運用、セキュリティ、失敗時の切り分けまで設計できる |

Part 0-11の完了は、上級の入口です。エキスパートになるには、このファイルの領域を実データと実運用で反復する必要があります。

## 実務課題

エキスパート編の最終課題として、`product-data-duckdb/` を次のように拡張します。

1. 入力manifestを追加し、各runで読んだファイル、サイズ、更新時刻、ハッシュを記録する
2. `run_id`、`batch_id`、`source_system` をraw/staging/martに通す
3. 品質チェック結果を履歴テーブルへ保存する
4. 出力Parquetをrun単位でバージョン化し、成功したrunだけをlatestとして公開する
5. スキーマ変更を検知し、許可された変更と失敗すべき変更を分ける
6. `EXPLAIN ANALYZE` とprofilingを保存し、遅くなった理由を比較できるようにする
7. secretをSQLから分離し、外部DB/S3接続の運用手順を書く
8. DuckDBで処理すべき部分と、DWHやアプリに渡すべき部分をADRとして記録する
9. dbt-duckdb、Dagster、dltを導入する前に、MakefileとSQLだけで固定すべき契約を説明する

この課題を完了できれば、DuckDBを単体ツールとしてではなく、実務のデータ処理基盤の一部として扱える状態に近づきます。

## 既存Partとの対応

| エキスパート領域 | 読み返すPart |
| --- | --- |
| DuckDBの位置づけ | [Part 0](parts/part_00_overview/README.md), [Part 10](parts/part_10_advanced/README.md) |
| ファイル取り込みとraw設計 | [Part 2](parts/part_02_file_ingestion/README.md), [Part 3](parts/part_03_cleaning_normalization/README.md) |
| SQLと分析設計 | [Part 4](parts/part_04_analytics_sql/README.md) |
| Python/Node.js境界 | [Part 5](parts/part_05_python/README.md), [Part 6](parts/part_06_node_app/README.md) |
| 外部DB/S3/HTTP | [Part 7](parts/part_07_external_sources/README.md) |
| 性能調査 | [Part 8](parts/part_08_performance/README.md) |
| 運用・テスト | [Part 9](parts/part_09_operations_testing/README.md) |
| 統合演習 | [Part 11](parts/part_11_final_project/README.md), [product-data-duckdb](product-data-duckdb/README.md) |

## 公式docsで深掘りする箇所

このファイルを読む段階では、公式docsをリファレンスとして使います。最初から全ページを読む必要はありません。自分が設計で迷った箇所を確認するために読みます。

- Performance Guide
- Profiling
- Configuration
- Parquet
- SQL Introduction
- Python Client
- Node.js Client
- Extensions
- PostgreSQL Extension
- httpfs / S3
- Operations Manual

重要なのは、公式docsの機能を暗記することではありません。自分の設計判断がDuckDBの前提と合っているかを確認することです。
